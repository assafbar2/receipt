{
  "name": "receipt",
  "version": "0.1.0",
  "private": true,
  "description": "Crash-safe execution for support agents: reconcile interrupted actions against provider receipts, or stop and hand off.",
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "start": "node --no-warnings src/server.mjs",
    "start:fixture": "PLANNER_MODE_OVERRIDE=fixture node --no-warnings src/server.mjs",
    "test": "node --no-warnings --test \"test/*.test.mjs\""
  }
}
